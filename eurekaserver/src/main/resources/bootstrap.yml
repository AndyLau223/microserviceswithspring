spring:
  application:
    name: eureka-server
  cloud:
    config:
#      specify configuration client address to load config.
      uri: http://localhost:8071
    loadbalancer:
      ribbon:
        enabled: false