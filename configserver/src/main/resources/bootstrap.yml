spring:
  application:
    name: config-server
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
#          specify where the configuration file storage.
          search-locations: classpath:/config

server:
  port: 8071